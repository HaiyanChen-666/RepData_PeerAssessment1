xlab = "Date",
ylab = "Average number of steps per day")
View(activity)
View(average_steps)
View(average_steps)
View(activity)
range(activity$interval)
# Calculated the total number of steps taken per day
total_steps <- aggregate(steps ~ date, data = activity, FUN = sum)
# Calculated the average number of steps taken per day
average_steps <- aggregate(steps ~ date, data = activity, FUN = mean)
# Calculated the median number of steps per day
median_steps <- aggregate(steps ~ date, data = activity, FUN = median)
View(median_steps)
View(average_steps)
# Calculated the total number of steps taken per day
total_steps <- aggregate(steps ~ date, data = activity, FUN = sum, na.rm = TRUE)
View(total_steps)
# Calculated the total number of steps taken per day
total_steps <- aggregate(steps ~ date, data = activity, FUN = sum, na.rm = TRUE)
# Calculated the average number of steps taken per day
average_steps <- aggregate(steps ~ date, data = activity, FUN = mean, na.rm = TRUE)
# Calculated the median number of steps per day
median_steps <- aggregate(steps ~ date, data = activity, FUN = median, na.rm = TRUE)
View(activity)
View(average_steps)
View(median_steps)
View(average_steps)
View(median_steps)
class(activity$steps)
class(activity$interval)
summary(activity$steps)
activity <- read.csv("activity.csv")
# check the class of the date column
class(activity$date)
# Calculated the total number of steps taken per day
total_steps <- aggregate(steps ~ date, data = activity, FUN = sum, na.rm = TRUE)
date_number <- as.numeric(as.Date(total_steps$date))
View(total_steps)
class(date_number)
# Make a histogram for the total number of steps per day
hist(total_steps$steps,
breaks = date_number,
main = "Total Number of Steps Per Day",
col = "orange",
xlab = "Date",
ylab = "Total Number of Steps (per day)")
View(total_steps)
# Make a histogram for the total number of steps per day
hist(total_steps$steps,
main = "Total Number of Steps Per Day",
col = "orange",
xlab = "Date",
ylab = "Total Number of Steps (per day)")
# Make a histogram for the total number of steps per day
hist(total_steps$steps,
breaks = seq(min(date_number), max(date_number), by = 1),
main = "Total Number of Steps Per Day",
col = "orange",
xlab = "Date",
ylab = "Total Number of Steps (per day)")
View(total_steps)
View(total_steps)
dates_x <- nrow(total_steps)
# Make a histogram for the total number of steps per day
hist(total_steps$steps,
breaks = dates_x,
main = "Total Number of Steps Per Day",
col = "orange",
xlab = "Date",
ylab = "Total Number of Steps (per day)")
# Make a histogram for the total number of steps per day
hist(total_steps$steps,
breaks = 20,
main = "Total Number of Steps Per Day",
col = "orange",
xlab = "Total Number of Steps (per day)",
ylab = "Number of days")
activity <- read.csv("activity.csv")
# check the class of the date column
class(activity$date)
# Calculated the total number of steps taken per day
total_steps <- aggregate(steps ~ date, data = activity, FUN = sum, na.rm = TRUE)
# Calculated the average number of steps taken per day
average_steps <- aggregate(steps ~ date, data = activity, FUN = mean, na.rm = TRUE)
# Calculated the median number of steps per day
median_steps <- aggregate(steps ~ date, data = activity, FUN = median, na.rm = TRUE)
# Time series plot of the average number of steps taken per day
library(ggplot2)
ggplot(average_steps,aes(x = date, y = steps)) +
geom_line(color = "darkblue") +  # Line plot
geom_point(color = "darkblue") + # Add points
labs(xlab = "Date",
ylab = "Average number of steps (per day)",
title = "Average number of steps by date") +
theme_bw()
# Time series plot of the average number of steps taken per day
library(ggplot2)
ggplot(average_steps,aes(x = date, y = steps)) +
geom_point(color = "darkblue") + # Add points
labs(xlab = "Date",
ylab = "Average number of steps (per day)",
title = "Average number of steps by date") +
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) + # horizontal justification
theme_bw()
ggplot(average_steps,aes(x = date, y = steps)) +
geom_point(color = "darkblue") + # Add points
geom_line(color = "darkblue") +  # Add line
labs(x = "Date",
y = "Average number of steps (per day)",
title = "Average number of steps by date") +
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) + # horizontal justification
theme_bw()
ggplot(average_steps,aes(x = date, y = steps, group = 1)) + # all points will be regarded as a group
geom_point(color = "darkblue") + # Add points
geom_line(color = "darkblue") +  # Add line
labs(x = "Date",
y = "Average number of steps (per day)",
title = "Average number of steps by date") +
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) + # horizontal justification
theme_bw()
ggplot(average_steps,aes(x = date, y = steps, group = 1)) + # all points will be regarded as a group
geom_point(color = "darkblue") + # Add points
geom_line(color = "darkblue") +  # Add line
labs(x = "Date",
y = "Average number of steps (per day)",
title = "Average number of steps by date") +
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) # horizontal justification
# Time series plot of the average number of steps taken per day
library(ggplot2)
ggplot(average_steps,aes(x = date, y = steps, group = 1)) + # all points will be regarded as a group
geom_point(color = "darkblue") + # Add points
geom_line(color = "darkblue") +  # Add line
labs(x = "Date",
y = "Average number of steps (per day)",
title = "Average Steps by Date") +
theme_bw() + # Add the theme before adjust the label for x-axis, or it will go to default setting (be horizontal)
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) # horizontal justification
View(activity)
# Find out the 5-minute interval that contains the maximum average steps
interval_avg <- aggregate(steps ~ interval, data = activity, FUN = mean)
interval_max <- interval_avg[which.max(interval_avg$steps),]
# Find out the 5-minute interval that contains the maximum average steps
interval_avg <- aggregate(steps ~ interval, data = activity, FUN = mean)
max_interval <- interval_avg[which.max(interval_avg$steps),]
max_interval
install.packages("DMwR")
# Code to describe and show a strategy for imputing missing data
install.packages("DMwR2")
mean(is.na(activity$steps))
install.packages("mice")
activity <- read.csv("activity.csv")
library(mice)  # 加载 mice 包
# 使用 PMM 方法进行多重插补，生成 12 个插补数据集
imp <- mice(activity, m = 12, method = 'pmm')
# 获取所有 12 个插补数据集，保存为一个列表
imputed_data_list <- lapply(1:12, function(i) complete(imp, i))
# 对每个插补数据集进行分析（例如线性回归）
pooled_results <- with(imp, lm(dependent_var ~ independent_var))
# 对每个插补数据集进行分析（例如线性回归）
pooled_results <- with(imp, lm(steps ~ interval * factor(date)))
# 汇总所有插补数据集的回归结果
pooled_summary <- pool(pooled_results)
View(pooled_summary)
# 查看汇总的回归分析结果
summary(pooled_summary)
View(pooled_results)
View(imputed_data_list)
library(MITools)
install.packages("MITools")
install.packages("mitools")
# Install necessary libraries
install.packages("mice")
# Load necessary libraries
library(mice)  # For multiple imputation
library(MITools)  # For pooling multiple imputation results
library(mice)  # For multiple imputation
library(mitools)  # For pooling multiple imputation results
# Perform multiple imputation using PMM (Predictive Mean Matching) method to generate 12 imputed datasets
# As it has around 13.2% missing values, I set m equal to 12. You could set m equal to 5 as default if the missing values is fewer.
imp <- mice(activity, m = 12, method = 'pmm')
# Retrieve all 12 imputed datasets and store them in a list
imputed_data_list <- lapply(1:12, function(i) complete(imp, i))
# Perform regression analysis (e.g., linear regression) on each imputed dataset
# Assume 'steps' is the dependent variable, 'interval' is the independent variable, and 'date' is a factor
pooled_results <- with(imp, lm(steps ~ interval * factor(date)))
# Pool the results from all imputed datasets
pooled_summary <- pool(pooled_results)
# View the summary of the pooled regression analysis results
summary(pooled_summary)
# If you want to calculate the total steps per day for each imputed dataset and plot a histogram, use the following code
# Calculate the total steps per day for all 12 imputed datasets
total_steps_list <- lapply(1:12, function(i) {
data_imputed <- complete(imp, i)  # Retrieve each imputed dataset
aggregate(steps ~ date, data = data_imputed, FUN = sum, na.rm = TRUE)  # Calculate total steps per day
})
# Combine all the imputed datasets' total steps into one data frame
total_steps_all <- do.call(rbind, total_steps_list)
# Plot a histogram of the total steps taken each day across all imputed datasets
hist(total_steps_all$steps,
main = "Total Steps Taken Each Day Across Pooled Imputed Datasets)",  # Title of the plot
col = "lightgreen",  # Color of the bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days")  # Y-axis label
imp$log
# Load necessary libraries
library(mice)  # For multiple imputation
library(mitools)  # For pooling multiple imputation results
# Perform multiple imputation using knn(K-Nearest Neighbors) method to generate 12 imputed datasets
# As it has around 13.2% missing values, I set m equal to 12. You could set m equal to 5 as default if the missing values is fewer.
imp <- mice(activity, m = 12, method = 'knn')
# Load necessary libraries
library(mice)  # For multiple imputation
library(mitools)  # For pooling multiple imputation results
# Perform multiple imputation using knn(K-Nearest Neighbors) method to generate 12 imputed datasets
# As it has around 13.2% missing values, I set m equal to 12. You could set m equal to 5 as default if the missing values is fewer.
imp <- mice(activity, m = 12, method = 'knn')
# Perform multiple imputation using PMM(Predictive Mean Matching) method to generate 12 imputed datasets
# As it has around 13.2% missing values, I set m equal to 12. You could set m equal to 5 as default if the missing values is fewer.
imp <- mice(activity, m = 12, method = 'PMM')
# Perform multiple imputation using PMM(Predictive Mean Matching) method to generate 12 imputed datasets
# As it has around 13.2% missing values, I set m equal to 12. You could set m equal to 5 as default if the missing values is fewer.
imp <- mice(activity, m = 12, method = 'pmm')
# Retrieve all 12 imputed datasets and store them in a list
imputed_data_list <- lapply(1:12, function(i) complete(imp, i))
# Perform regression analysis (e.g., linear regression) on each imputed dataset
# Assume 'steps' is the dependent variable, 'interval' is the independent variable, and 'date' is a factor
pooled_results <- with(imp, lm(steps ~ interval * factor(date)))
# Pool the results from all imputed datasets
pooled_summary <- pool(pooled_results)
# View the summary of the pooled regression analysis results
summary(pooled_summary)
# If you want to calculate the total steps per day for each imputed dataset and plot a histogram, use the following code
# Calculate the total steps per day for all 12 imputed datasets
total_steps_list <- lapply(1:12, function(i) {
data_imputed <- complete(imp, i)  # Retrieve each imputed dataset
aggregate(steps ~ date, data = data_imputed, FUN = sum, na.rm = TRUE)  # Calculate total steps per day
})
# Combine all the imputed datasets' total steps into one data frame
total_steps_all <- do.call(rbind, total_steps_list)
# Plot a histogram of the total steps taken each day across all imputed datasets
hist(total_steps_all$steps,
main = "Total Steps Taken Each Day Across Pooled Imputed Datasets)",  # Title of the plot
col = "lightgreen",  # Color of the bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days")  # Y-axis label
# Perform multiple imputation using PMM(Predictive Mean Matching) method to generate 12 imputed datasets
# As it has around 13.2% missing values, I set m equal to 12. You could set m equal to 5 as default if the missing values is fewer.
imp <- mice(activity, m = 12, method = 'KNN')
?mice
methods(mice)
install.packages(VIM)
install.packages("VIM")
install,packages("DMwR2")
install.packages("DMwR2")
library(devtools)
install.packages("devtools")
install.packages("devtools")
library(devtools)
install_github("ltorgo/DMwR2", ref="master")
install.packages("imputeTS")
# Use knn method for imputation
imputed_data <- kNN(acitivty)
# Load necessary packages
library(imputeTS)
# Use knn method for imputation
imputed_data <- kNN(acitivty)
> # Load necessary packages
installed.packages()["imputeTS", ]
# We could also use knn(K-Nearest Neighbors), another mutiple imputation method, for imputing missing data
#
activity$Date <- as.Date(activity$Date)
View(activity)
# We could also use knn(K-Nearest Neighbors), another mutiple imputation method, for imputing missing data
#
activity$date <- as.Date(activity$date)
# Load necessary packages
library(imputeTS)
# Use knn method for imputation
imputed_data <- kNN(acitivty)
# Use knn method for imputation
imputed_data <- kNN(activity)
# Use knn method for imputation
imputed_data <- kNN(activity, k = 5)
# Load necessary packages
library(imputeTS)
# Use knn method for imputation
imputed_data <- kNN(activity, k = 5)
library("VIM")
install.packages("VIM")
# Load necessary packages
library(VIM)
# Use knn method for imputation
imputed_data <- kNN(activity, k = 5)
activity <- read.csv("activity.csv")
# Load necessary packages
library(VIM)
# Use knn method for imputation
imputed_data <- kNN(activity, k = 5)  # set the number of nearest neighbor equal to 5
View(imputed_data)
View(imputed_data)
View(activity)
View(imputed_data)
View(activity)
View(imputed_data)
# Use knn method for imputation
knn_imputed_data <- kNN(activity, k = 5)  # set the number of nearest neighbor equal to 5
# Use knn method for imputation
imputed_data <- kNN(activity, k = 5)  # set the number of nearest neighbor equal to 5
# Calculate total steps per day
knn_imputed_data_sum <- aggregate(steps ~ date, data = imputed_data, FUN = sum, na.rm = TRUE)
# Plot a histogram of the total steps taken each day using imputed data
hist(knn_imputed_data_sum$steps,
main = "Total Steps Taken Each Day in Imputed dataset)",  # Title of the plot
col = "lightgreen",  # Color of the bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days")  # Y-axis label
# Plot a histogram of the total steps taken each day using imputed data
hist(knn_imputed_data_sum$steps,
main = "Total Steps Taken Each Day in Imputed dataset)",  # Title of the plot
col = "purple",  # Color of the bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days")  # Y-axis label
# Plot a histogram of the total steps taken each day using imputed data
hist(knn_imputed_data_sum$steps,
main = "Total Steps Taken Each Day in Imputed dataset)",  # Title of the plot
col = "lightskyblue",  # Color of the bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days")  # Y-axis label
# Panel plot comparing the average number of steps taken per 5-minute interval across weekdays and weekends
# Firstly, we need to add a new column named weekdays which based on the date column
activity$weekdays <- wday(as.Date(activity$date))
# Panel plot comparing the average number of steps taken per 5-minute interval across weekdays and weekends
# Firstly, we need to add a new column named weekdays which based on the date column
# Load the package for process date
library(lubridate)
activity$weekdays <- wday(as.Date(activity$date))
View(activity)
activity$weekdays <- weekdays(as.Date(activity$date))
View(activity)
# Panel plot comparing the average number of steps taken per 5-minute interval across weekdays and weekends
# Set the language in English
Sys.setlocale("LC_ALL", "English_United States.UTF-8")
# Firstly, we need to add a new column named weekdays which based on the date column
# Load the package for process date
library(lubridate)
activity$weekdays <- weekdays(as.Date(activity$date))
View(activity)
# Calculated the average number of steps taken per day
average_steps_d <- aggregate(steps ~ weekdays, data = activity, FUN = mean, na.rm = TRUE)
View(average_steps_d)
# Create a new column named group
activity$group <- ifelse(activity$weekdays %in% c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday"), "weekdays", "weekend")
View(activity)
# Calculated the average number of steps taken per day
average_steps_d <- aggregate(steps ~ interval + group, data = activity, FUN = mean, na.rm = TRUE)
View(average_steps_d)
View(average_steps_d)
activity <- read.csv("activity.csv")
# Panel plot comparing the average number of steps taken per 5-minute interval across weekdays and weekends
# Set the language in English
Sys.setlocale("LC_ALL", "English_United States.UTF-8")
# Firstly, we need to add a new column named weekdays which based on the date column
# Load the package for process date
library(lubridate)
# Create a new column named weekdays
activity$weekdays <- weekdays(as.Date(activity$date))
# Create a new column named group
activity$group <- ifelse(activity$weekdays %in% c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday"), "weekdays", "weekend")
# Calculated the average number of steps taken per day
average_steps_d <- aggregate(steps ~ interval + group, data = activity, FUN = mean, na.rm = TRUE)
library(ggplot2)
ggplot(activity, aes(x = interval, y = steps)) +
stat_summary(fun = "mean", geom = "line") +
facet_grid(. ~ group) +
labs(x = "Time (5-min intervals)", y = "Average Steps") +
theme_bw()
View(activity)
ggplot(activity, aes(x = interval, y = steps)) +
stat_summary(fun = "mean",na.rm = TRUE, geom = "line") +
facet_grid(. ~ group) +
labs(x = "Time (5-min intervals)", y = "Average Steps") +
theme_bw()
# Create a new column named group
activity$group <- ifelse(activity$weekdays %in% c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday"), "weekday", "weekend")
library(ggplot2)
ggplot(activity, aes(x = interval, y = steps)) +
stat_summary(fun = "mean",na.rm = TRUE, geom = "line") +
facet_grid(. ~ group) +
labs(x = "Time (5-min intervals)", y = "Average Steps") +
theme_bw()
# Load necessary packages
library(mice)  # For multiple imputation
library(mitools)  # For pooling multiple imputation results
library(VIM)  # For KNN imputation
# Ensure 'date' column is in Date format
activity$date <- as.Date(activity$date)
# Perform multiple imputation using PMM(Predictive Mean Matching) method to generate 12 imputed datasets
imp <- mice(activity, m = 12, method = 'pmm')
# Retrieve all 12 imputed datasets and store them in a list
imputed_data_list <- lapply(1:12, function(i) complete(imp, i))
# Perform regression analysis (e.g., linear regression) on each imputed dataset
pooled_results <- with(imp, lm(steps ~ interval * factor(date)))
# Pool the results from all imputed datasets
pooled_summary <- pool(pooled_results)
# View the summary of the pooled regression analysis results
summary(pooled_summary)
# Plot diagnostic plots for imputation process
plot(imp)  # Show diagnostics of the imputation process
activity <- read.csv("activity.csv")
# Load necessary packages
library(mice)  # For multiple imputation
library(mitools)  # For pooling multiple imputation results
library(VIM)  # For KNN imputation
# Ensure 'date' column is in Date format
activity$date <- as.Date(activity$date)
# Perform multiple imputation using PMM(Predictive Mean Matching) method to generate 12 imputed datasets
imp <- mice(activity, m = 12, method = 'pmm')
# Retrieve all 12 imputed datasets and store them in a list
imputed_data_list <- lapply(1:12, function(i) complete(imp, i))
# Perform regression analysis (e.g., linear regression) on each imputed dataset
pooled_results <- with(imp, lm(steps ~ interval * factor(date)))
# Pool the results from all imputed datasets
pooled_summary <- pool(pooled_results)
# View the summary of the pooled regression analysis results
summary(pooled_summary)
# Calculate total steps per day for all 12 imputed datasets
total_steps_list <- lapply(1:12, function(i) {
data_imputed <- complete(imp, i)  # Retrieve each imputed dataset
aggregate(steps ~ date, data = data_imputed, FUN = sum, na.rm = TRUE)  # Calculate total steps per day
})
# Combine all the imputed datasets' total steps into one data frame
total_steps_all <- do.call(rbind, total_steps_list)
# Plot histograms to compare the distribution of data before and after imputation
par(mfrow = c(1, 3))  # Split the plot window into 1 row and 3 columns
# Histogram before imputation (original data)
hist(activity$steps,
main = "Histogram Before Imputation",  # Title for the histogram before imputation
col = "lightblue",  # Color of the histogram bars
xlab = "Steps",  # X-axis label
ylab = "Frequency",  # Y-axis label
breaks = 20)  # Set the number of bins for the histogram
# Histogram after imputation - PMM method
# Plot a histogram of the total steps taken each day across all imputed datasets
hist(total_steps_all$steps,
main = "Total Steps Taken Each Day Across Pooled Imputed Datasets (PMM)",  # Title for the histogram after imputation
col = "lightgreen",  # Color of the histogram bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days",  # Y-axis label
breaks = 20)  # Set the number of bins for the histogram
# Use knn(K-Nearest Neighbors), another multiple imputation method, for imputing missing data
imputed_data_knn <- kNN(activity, k = 5)  # Set the number of nearest neighbors equal to 5
# Calculate total steps per day for KNN-imputed data
knn_imputed_data_sum <- aggregate(steps ~ date, data = imputed_data_knn, FUN = sum, na.rm = TRUE)
# Plot a histogram of the total steps taken each day using imputed data (KNN)
hist(knn_imputed_data_sum$steps,
main = "Total Steps Taken Each Day in KNN Imputed Dataset",  # Title of the plot
col = "lightskyblue",  # Color of the bars
xlab = "Total Steps Per Day",  # X-axis label
ylab = "Number of Days",  # Y-axis label
breaks = 20)  # Set the number of bins for the histogram
# Calculated the average number of steps per day
average_steps <- aggregate(steps ~ date, data = activity, FUN = mean, na.rm = TRUE)
View(average_steps)
# Time series plot of the average number of steps taken per day
library(ggplot2)
ggplot(average_steps,aes(x = date, y = steps, group = 1)) + # all points will be regarded as a group
geom_point(color = "darkblue") + # Add points
geom_line(color = "darkblue") +  # Add line
labs(x = "Date",
y = "Average number of steps (per day)",
title = "Average Steps by Date") +
theme_bw() + # Add the theme before adjust the label for x-axis, or it will go to default setting (be horizontal)
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) # horizontal justification
View(activity)
setwd("E:/新技能/Epidemiology/John_Hopkins_Reproducible_research/RepData_PeerAssessment1")
unzip("activity.zip", exdir = "E:/新技能/Epidemiology/John_Hopkins_Reproducible_research/RepData_PeerAssessment1")
activity <- read.csv("activity.csv")
# Calculated the total number of steps per day
total_steps <- aggregate(steps ~ date, data = activity, FUN = sum, na.rm = TRUE)
# Make a histogram for the total number of steps per day
hist(total_steps$steps,
breaks = 20,
main = "Total Number of Steps Per Day",
col = "orange",
xlab = "Total Number of Steps (per day)",
ylab = "Number of days")
# Calculated the average number of steps per day
average_steps <- aggregate(steps ~ date, data = activity, FUN = mean, na.rm = TRUE)
# Calculated the median number of steps per day
median_steps <- aggregate(steps ~ date, data = activity, FUN = median, na.rm = TRUE)
# Time series plot of the average number of steps taken per day
library(ggplot2)
ggplot(average_steps,aes(x = date, y = steps, group = 1)) + # all points will be regarded as a group
geom_point(color = "darkblue") + # Add points
geom_line(color = "darkblue") +  # Add line
labs(x = "Date",
y = "Average number of steps (per day)",
title = "Average Steps by Date") +
theme_bw() + # Add the theme before adjust the label for x-axis, or it will go to default setting (be horizontal)
theme(axis.text.x = element_text(angle = 45,  # 45-degree rotation
hjust = 1)) # horizontal justification
activity <- read.csv("activity.csv")
# Calculated the average number of steps per day
average_steps <- aggregate(steps ~ date, data = activity, FUN = mean, na.rm = TRUE)
average_steps
